# Netflora Online – dependências do app Streamlit
# Instale com: pip install -r requirements.txt

# Interface web
streamlit>=1.35.0

# Manipulação de dados
pandas>=1.5.0
numpy>=1.23.0

# Imagens e geoespacial
Pillow>=9.0.0
rasterio>=1.3.0

# Mapa interativo
folium>=0.16.0
streamlit-folium>=0.20.0

# Exportação de shapefile (fiona = backend padrão do geopandas)
geopandas>=0.13.0
shapely>=2.0.0
fiona>=1.9.0

# Requisições HTTP (download de pesos / repo)
requests>=2.28.0

# Dependências do detect.py do Netflora (modelo YOLO)
# Versão CPU — Streamlit Cloud não tem GPU
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.5.0
PyYAML>=6.0
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0
